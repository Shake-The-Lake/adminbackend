version: "3.8"

services:
  backend:
    image: maven:3.9.0-eclipse-temurin-19
    build: !reset null
    command: ["bash", "-c" , "while true; do sleep 10; done"]

    volumes:
      - .:/shakethelakebackend:cached

    ports:
      - "8080:8080"
  
